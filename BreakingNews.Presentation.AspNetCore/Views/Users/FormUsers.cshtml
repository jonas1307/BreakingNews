@model BreakingNews.Application.ViewModels.UserViewModel

@{
    ViewBag.Title = $"{(string.IsNullOrEmpty(Model.Id) ? "Cadastro" : "Alteração")} de Usuário";
    Layout = "_Layout";
}

<h2>@ViewBag.Title</h2>

@using (Html.BeginForm("Save", "Users", FormMethod.Post))
{
    @Html.AntiForgeryToken()

    <div class="form-group">
        @Html.LabelFor(m => m.Id)
        @Html.TextBoxFor(m => m.Id, new { @readonly = "", @class = "form-control", autocomplete = "off" })
        @Html.ValidationMessageFor(m => m.Id)
    </div>

    <div class="form-group">
        @Html.LabelFor(m => m.UserName)
        @Html.TextBoxFor(m => m.UserName, new { @readonly = "", @class = "form-control", autocomplete = "off" })
        @Html.ValidationMessageFor(m => m.UserName)
    </div>

    <div class="form-group">
        @Html.LabelFor(m => m.SelectedRoles)
        @Html.ListBoxFor(m => m.SelectedRoles, Model.RolesListItems)
        @Html.ValidationMessageFor(m => m.SelectedRoles)
    </div>

    <div class="form-group">
        <button class="btn btn-success" type="submit">Salvar</button>
        <a href="@Url.Action("Index", "News")" class="btn btn-danger">Voltar</a>
    </div>
}
